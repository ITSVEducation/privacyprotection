# 個人情報マスキングアプリ 設計書

作成日: 2026-07-23
改訂: 2026-07-23 Codexレビュー指摘を反映（Rev.2）
ステータス: ドラフト（ユーザーレビュー待ち）

## 1. 背景・目的

社内でAIサービスにテキストやファイルを渡す際、個人情報や会社固有の機密情報を含めることができない。人手による事前チェックは負担が大きいため、**完全ローカルで動作する自動マスキングアプリ**を作成する。マスクした情報はAIの回答受領後に元へ復元できるようにする。

### 1.1 脅威モデル（守るもの・守らないもの）

本アプリの目的は「**個人情報・機密情報がクラウド上のAIサービスに渡ること**」を防ぐことである。

- **守る**: マスク済ファイル・テキストに個人情報が残ってAIに送信されること
- **守らない**: ローカルPC内に個人情報が存在すること自体（元ファイルも対応表もローカルにあることは前提であり問題としない）

この前提から、対応表は**暗号化せず、人間が見て分かりやすい平文形式**とする。PC盗難・マルウェア等によるローカルデータ漏洩は本アプリのスコープ外であり、OSレベルの対策（BitLocker等）でカバーする。

## 2. 要件

### 2.1 機能要件

| # | 要件 |
|---|------|
| F-1 | 定型PII（電話番号・メールアドレス・住所・郵便番号・マイナンバー・クレジットカード番号等）を正規表現パターンで検出する |
| F-2 | カスタム辞書（取引先名・製品コード・社内用語等）に登録された語句を検出する |
| F-3 | 日本語固有表現抽出（NER）により、辞書未登録の人名・組織名・地名を検出する |
| F-4 | テキスト系ファイル（.txt / .csv / .json / .md 等のプレーンテキスト）をマスクできる |
| F-5 | Office系ファイル（.xlsx / .docx / .pptx）を書式を保持したままマスクできる |
| F-6 | クリップボードのテキストを直接マスクし、クリップボードに戻せる |
| F-7 | 指定フォルダ配下を再帰的に一括マスクできる |
| F-8 | マスク方式を「可逆（代替トークン＋対応表）」「不可逆（塗りつぶし）」から選択できる |
| F-9 | 可逆方式では対応表（人間可読なCSV）を出力し、マスク済ファイルまたはAI回答テキストを元に復元できる |
| F-10 | マスク実行前に検出結果をプレビューし、誤検出の除外・検出漏れの手動追加ができる |
| F-11 | 「確認なしで即変換」オプションを選択できる。即変換時も処理後レポートを必ず表示する（→ 6.4） |
| F-12 | カスタム辞書をGUIで編集でき、CSVインポートに対応する |
| F-13 | 検出カテゴリ（人名・組織名・地名等）を個別にON/OFFできる |

### 2.2 非機能要件

| # | 要件 |
|---|------|
| N-1 | 完全ローカル実行。ネットワーク通信を行うコードを一切含めない（→ 9.1） |
| N-2 | Windows専用。単体exe（またはワンフォルダ）形式で配布。MLモデル同梱でサイズ500MB前後を許容 |
| N-3 | ITに詳しくない社員でも使えるGUI（ドラッグ&ドロップ中心） |
| N-4 | 大きなファイルの処理中もUIが固まらない（ワーカースレッド＋進捗表示） |
| N-5 | NER検出は完全ではないことをUI上に常時注記する |
| N-6 | 元ファイルは決して変更しない。出力は常に別ファイル |

### 2.3 対象外（スコープ外）

- PDFファイル（テキスト層抽出・OCRとも対象外）
- 埋込画像内の文字（OCRしない）
- Mac / Linux対応
- 対応表の暗号化（1.1 の脅威モデルにより不要と判断）
- ローカルPC自体のセキュリティ（盗難・マルウェア対策）
- 社内サーバー・Web版

## 3. 技術選定

| 項目 | 選定 | 理由 |
|------|------|------|
| 言語 | Python 3.11+ | 日本語NERエコシステムが最も充実 |
| GUI | PySide6 (Qt) | ドラッグ&ドロップ、リッチなハイライト表示、実績 |
| NER | GiNZA (spaCy + ja_ginza) | 完全オフライン動作の日本語固有表現抽出 |
| xlsx | openpyxl | セル・コメント・ヘッダーフッターにアクセス可能 |
| docx | python-docx | 段落・表・ヘッダーフッターの書式保持置換 |
| pptx | python-pptx | スライド・ノートのテキスト置換 |
| パッケージング | PyInstaller | Windows単体配布 |
| テスト | pytest | コア層のユニットテスト |

## 4. アーキテクチャ

3層構成とし、**コア層はGUIに一切依存しない**。

```
┌─────────────────────────────────────────┐
│  GUI層 (PySide6)                          │
│  メイン画面 / プレビュー画面 / 設定画面 /    │
│  処理後レポート画面                         │
├─────────────────────────────────────────┤
│  コア層 (純Python・GUI非依存)               │
│  ・Detector: 検出エンジン                   │
│    - PatternDetector（正規表現）            │
│    - DictionaryDetector（カスタム辞書）      │
│    - NerDetector（GiNZA）                  │
│  ・Masker: 置換エンジン（可逆／不可逆）       │
│  ・Restorer: 復元エンジン（対応表→逆置換）    │
├─────────────────────────────────────────┤
│  ファイルI/O層 (Handler共通インターフェース)   │
│  TextHandler / XlsxHandler / DocxHandler / │
│  PptxHandler / ClipboardHandler /          │
│  FolderWalker（再帰一括処理）               │
└─────────────────────────────────────────┘
```

### 4.1 コア層の主要インターフェース

```python
@dataclass
class Detection:
    text: str          # 検出された文字列（例: "山田太郎"）
    category: str      # 種別（PERSON / ORG / LOC / PHONE / EMAIL / ADDRESS / CUSTOM 等）
    start: int         # テキスト断片内の開始位置
    end: int           # 終了位置
    source: str        # 検出元（pattern / dictionary / ner）
    enabled: bool      # プレビューでの採否（既定 True）

class Detector:
    def detect(self, text: str) -> list[Detection]: ...

class Masker:
    def mask(self, fragments: list[str], detections: list[list[Detection]],
             mode: Literal["token", "redact"]) -> tuple[list[str], MappingTable]: ...

class Restorer:
    def restore(self, text: str, mapping: MappingTable) -> RestoreResult: ...
    # RestoreResult = 復元後テキスト + 未知トークン一覧（警告表示用）
```

### 4.2 検出範囲の重複解決ルール

複数の検出器が重複する範囲を検出した場合、以下の順で一意に解決する。

1. **範囲が長い方を採用**（例:「株式会社山田製作所」(ORG) と「山田」(PERSON) が重複 → 前者）
2. 同じ長さなら検出元の優先度 **dictionary > pattern > ner** で採用
3. 部分的に重なり合い包含関係にない場合は、開始位置が先のものを採用し、後のものは重複しない残り部分を再検出

このルールは Detector のマージ処理として実装し、ユニットテストで固定する。

### 4.3 検出の優先順位（設定・手動操作との関係）

適用順は **プレビューでの手動操作 ＞ カテゴリON/OFF設定 ＞ 検出器**。

- カテゴリOFFの種別は検出リストに現れない
- ただしプレビューでの手動追加は、カテゴリ設定に関係なく常に有効
- プレビューでの個別除外は、その処理1回に限り有効（設定には影響しない）

### 4.4 トークン仕様と衝突対策

- トークン文法: `【<種別ラベル>_<連番>】`（例: `【人名_1】`【組織_3】`）。種別ラベルは日本語固定語彙（人名/組織/地名/電話/メール/住所/番号/カスタム）
- 同一の値には処理単位（単一ファイル or フォルダ一括の全体）で同一トークンを割り当てる
- **衝突対策**: マスク前に原文をトークン文法の正規表現でスキャンし、既にトークン形式の文字列が含まれる場合は警告を表示して連番をその後ろから開始する（誤復元を防止）
- **復元は完全一致のみ**: AI回答中のトークンは文字単位の完全一致で逆置換する。AIがトークンを改変した場合（例: 【人名_1】→【人名1】）は復元されず、未知トークン・残存トークンとして警告一覧に表示する。曖昧マッチによる誤復元はしない

### 4.5 ファイルI/O層の共通インターフェース

```python
class FileHandler(ABC):
    extensions: ClassVar[list[str]]
    def read_fragments(self, path: Path) -> list[Fragment]: ...
    def write_fragments(self, src: Path, dst: Path,
                        masked: list[Fragment]) -> None: ...
    # Fragment = ファイル内の位置情報（セル番地・段落番号等）付きテキスト断片。
    # write は元ファイルをコピーした上でテキスト部分のみ差し替え、書式を保持する
```

新しい拡張子への対応は Handler を1クラス追加するだけで完結させる。

### 4.6 Office系ファイルのマスク対象範囲

「どこまで見るか」を形式ごとに明示する。**対象外の箇所は処理後レポートに注記として常に表示**する。

| 形式 | マスク対象 | 対象外（レポートに注記） |
|------|-----------|------------------------|
| .xlsx | セル値・数式内の文字列リテラル・セルコメント・ヘッダーフッター・シート名・定義名 | 図形/テキストボックス内文字（openpyxlの制約により将来対応）・埋込画像・埋込オブジェクト・ピボットキャッシュ |
| .docx | 本文段落・表・ヘッダーフッター・脚注・文末脚注・コメント | テキストボックス/図形内文字（将来対応）・埋込画像・埋込オブジェクト・変更履歴の削除済みテキスト |
| .pptx | スライド内シェイプのテキスト・表・スピーカーノート | 埋込画像・埋込オブジェクト・スライドマスター |

- トークン置換によりセル幅・レイアウトが崩れる可能性は許容する（AIに渡すための中間物であり、見た目の完全性は要件としない）
- 数式内リテラルを置換した場合、数式が壊れる可能性があるため、置換箇所をレポートに列挙する

### 4.7 文字コード・改行の扱い

- テキスト系ファイルは読み込み時にエンコーディングを判定（UTF-8 BOM / UTF-8 / CP932 の順に試行、charset-normalizer で補助）し、**出力は元のエンコーディング・改行コードを維持**する
- Unicode正規化（NFC/NFKC）は**行わない**。原文のバイト列忠実性を優先し、マスク→復元のラウンドトリップで完全一致を保証する
- 判定不能なファイルはスキップしてエラー一覧に表示

### 4.8 対応表（マッピングファイル）仕様

- ファイル名: `<出力ファイル名>.pmap.csv`（マスク済ファイルと同じフォルダに並置）
- 形式: **UTF-8 BOM付きCSV**（Excelでダブルクリックで開ける。人間が確認する用途を第一とする）
- 列: `トークン, 元の値, 種別, 出現回数`
- 元の値にカンマ・改行を含む場合は RFC 4180 に従いダブルクォートで囲む（Excelも正しく解釈する）

```csv
トークン,元の値,種別,出現回数
【人名_1】,山田太郎,人名,5
【組織_1】,株式会社サンプル,組織,2
【メール_1】,yamada@example.co.jp,メール,1
```

- 1行目はヘッダー行（復元時はヘッダーを検証して読み飛ばす）
- フォルダ一括処理時は、処理全体で1つの共有対応表を使い（同一人物は全ファイルで同一トークン）、ルートに `_folder.pmap.csv` を出力する
- 不可逆モードでは対応表を出力しない

## 5. データフロー

### 5.1 マスク時

1. ユーザーがファイル／フォルダをドロップ、またはクリップボード読込
2. Handler がテキスト断片（Fragment）を抽出
3. Detector が検出リストを作成（重複解決済み）
4. プレビュー画面で原文ハイライト表示 → ユーザーが除外／手動追加（「確認なし」時はスキップ）
5. Masker が置換実行
6. 出力: `<元名>_masked.<拡張子>` ＋（可逆時）`.pmap.csv`。元ファイルは変更しない
7. **処理後レポートを表示**（→ 6.4）

### 5.2 復元時

1. 復元対象を指定: マスク済ファイル、または AI回答を貼り付けたテキスト／クリップボード
2. 対応する `.pmap.csv` を指定（同フォルダにあれば自動検出）
3. Restorer がトークンを完全一致で逆置換。対応表にないトークン・置換されず残ったトークンは警告一覧に表示

### 5.3 出力の原子性（クラッシュ・キャンセル対策）

- マスク済ファイル・対応表は**一時ファイルに書き込み完了後、リネームで確定**する（部分書き込みされた出力を残さない）
- 対応表はマスク済ファイルより**先に**確定する（マスク済ファイルだけ存在して復元不能になる状態を作らない）
- キャンセル・クラッシュ時は一時ファイルを削除。処理済みの完成ペア（マスク済＋対応表）は残す
- フォルダ一括のキャンセル時は、完了済みファイル一覧をレポートに表示

### 5.4 フォルダ再帰処理の境界条件

- シンボリックリンク・ジャンクションは**辿らない**（無限ループ防止）
- 隠しファイル・システムファイル・`~$` で始まるOfficeロックファイルはスキップ
- 未対応拡張子はスキップし、レポートに件数と一覧を表示（「処理されなかったファイル」を明示することで、未処理ファイルをAIに渡してしまう事故を防ぐ）
- 出力ファイル（`_masked` 付き・`.pmap.csv`）自体は再処理の対象にしない

## 6. 画面構成

### 6.1 メイン画面

- 中央: ドラッグ&ドロップゾーン（ファイル複数可・フォルダ可）
- モード切替: マスク ／ 復元
- マスク方式: 可逆（トークン） ／ 不可逆（塗りつぶし）
- チェックボックス: 「確認なしで即変換」
- クリップボード操作: 「貼り付け→マスク→コピー」ワンクリックボタン
- 常時注記: 「自動検出は完全ではありません。重要なデータは必ずプレビューで確認してください」

### 6.2 プレビュー画面

- 左ペイン: 原文表示。検出箇所を種別ごとに色分けハイライト
- 右ペイン: 検出一覧（種別・値・件数）。チェックボックスで個別除外、種別単位の一括除外
- 手動追加: 左ペインでテキスト範囲選択 → 右クリック → 種別を選んで追加
- フォルダ一括時: ファイルごとにタブ表示（多数の場合は確認なしモードを促す）
- 確定ボタンで置換実行

### 6.3 設定画面

- カスタム辞書編集: 語句＋種別の表形式編集、CSVインポート／エクスポート（列: `語句, 種別`、UTF-8 BOM付き、重複語句はインポート時に警告）
- 検出カテゴリのON/OFF（例: 地名を検出しない）
- 出力先設定: 元ファイルと同じフォルダ（既定） ／ 固定フォルダ
- 設定・辞書の保存先: `%APPDATA%/PrivacyProtection/`（JSON）

### 6.4 処理後レポート（即変換時の安全担保）

マスク実行後（プレビュー経由・即変換とも）、必ずレポートを表示する。

- ファイルごとの検出件数・種別内訳（例: 人名5件、電話番号2件）
- **検出0件のファイルを強調表示**し、「検出漏れの可能性があります。内容を確認してください」と注記
- マスク対象外の要素（図形内テキスト等）が存在したファイルの注記
- スキップされたファイル一覧（未対応拡張子・エラー）
- フォルダ一括時は同内容を `_report.txt` としてルートに出力
- レポートには**元の値そのものは記載しない**（件数・種別のみ。値の確認はプレビューまたは対応表で行う）

## 7. エラー処理

| ケース | 挙動 |
|--------|------|
| パスワード付き・破損Officeファイル | スキップし、処理完了後にレポートで表示（一括処理は継続） |
| エンコーディング判定不能なテキストファイル | スキップし、レポートで表示 |
| 未対応拡張子（フォルダ一括時） | スキップし、件数と一覧をレポートに表示 |
| 復元時に対応表にないトークン／残存トークン | そのまま残し、警告一覧に表示 |
| 原文に既存のトークン形式文字列 | 警告表示し、連番をその後ろから開始（4.4） |
| 巨大ファイル | ワーカースレッドで処理、進捗バー表示、キャンセル可能 |
| 出力先に同名ファイルが存在 | 連番付与（`_masked(2)` 等）で上書きしない |
| クラッシュ・キャンセル | 一時ファイル方式により部分書き込みを残さない（5.3） |

エラーメッセージ・ログにはファイルパスと理由のみを記録し、検出された値そのものは記録しない。

## 8. テスト方針

- コア層（Detector / Masker / Restorer）: pytest でユニットテスト
  - パターン検出: 各PII種別の正例・負例（形式は正しいが無効な番号等の境界ケースを含む）
  - 重複解決: 4.2 のルールを網羅するケース
  - トークン一貫性: 同一値→同一トークン、フォルダ一括での横断一貫性
  - トークン衝突: 原文にトークン形式文字列が含まれるケース
  - **ラウンドトリップ: マスク→復元で原文と完全一致することを全対応形式・全エンコーディング（UTF-8/CP932/改行コード違い）で検証**
  - 復元: AIがトークンを改変したケースで誤復元しない（未知トークン警告になる）こと
- ファイルI/O層: 実ファイルフィクスチャ（xlsx/docx/pptx/txt/csv）で書式保持・対象範囲（4.6）を検証
- NER: 精度そのものはテストせず、「検出結果がDetection形式で返る」結合レベルの確認に留める
- レポート: 検出0件ファイルの強調・スキップ一覧の表示を検証
- GUI: 手動確認中心

## 9. 配布・実行環境の保証

### 9.1 「ネットワーク通信をしない」の担保

- ネットワーク通信を行うライブラリ・コードを一切含めない（NERモデルは配布物に同梱し、実行時ダウンロードをしない）
- 依存ライブラリの実行時通信（spaCyのテレメトリ等）は設定で無効化し、ビルド時に確認する
- CIまたはリリース前チェックで、`socket` / `urllib` / `requests` 等のネットワークAPI呼び出しが自社コードに含まれないことを静的検査する
- 追加の保証が必要な環境では、Windows Firewall でアプリの外向き通信をブロックする運用を配布手順書に記載する

### 9.2 パッケージング

- PyInstaller によるビルド（GiNZAモデル同梱、想定サイズ 500MB前後）
- 起動時間短縮のため one-folder 形式を第一候補とし、one-file（単体exe）は起動速度を検証の上で判断
- インストーラー不要（ZIP展開のみで動作）
- 配布ZIPには SHA-256 ハッシュを添付し、改ざん確認に使えるようにする

## 10. 将来拡張（今回は実装しない）

- PDF対応（テキスト層・OCR）
- Office図形/テキストボックス内テキストのマスク（4.6 の対象外箇所）
- マスク履歴の一元DB管理
- 検出品質の受け入れ基準策定（社内サンプル文書での検出率測定）
